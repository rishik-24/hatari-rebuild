import React from "react";
import { StyleSheet, Text, View } from "react-native";

const menu = () => {
  return (
    <View>
      <Text>menu</Text>
    </View>
  );
};

export default menu;

const styles = StyleSheet.create({});
